# Add a Network Printer by IP

**Goal:** Install by TCP/IP with correct driver; print a test page.

## Steps (High level)
Settings → Printers & Scanners → Add device → **Add manually** → TCP/IP → IP → driver → Test page.

## Artifacts
- `screenshots/` (wizard, driver, success)
- `docs/notes.md` (model, driver source link, IP used, fixes)
