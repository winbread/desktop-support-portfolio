# Break/Fix Triage (Windows)

**Flow:** ipconfig → ping → tracert/pathping → DNS check → driver/device mgr.

**Artifacts**
- `screenshots/` (command outputs + fix)
- `triage-flow.png` (simple flowchart)
- Sample ticket note (Problem → Actions → Result)
