# Add a Network Printer by IP
Goal: install via TCP/IP with the correct driver; print a test page.
Path: Settings → Printers & Scanners → Add device → Add manually → TCP/IP.
Put screenshots in ./screenshots/
